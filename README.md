# EPUB Author Organizer

A Python tool that automatically organizes EPUB files into author-based folders.

## Features

- Reads EPUB metadata
- Creates author folders automatically
- Moves books into correct directories
- Handles missing metadata (Unknown_Author)

## Installation

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
